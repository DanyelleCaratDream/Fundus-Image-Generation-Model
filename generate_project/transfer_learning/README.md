# transfer_learning/ —— 迁移学习方法

> Phase C（Bug 2 拓展）：利用预训练权重/其他任务权重迁移到眼底图生成或特征提取。

**状态**：📌 占位，尚未开始。与 `pretrained/`（预训练生成）区分：这里侧重"迁移"范式（权重初始化、域适配、特征提取器复用）。

**参考**：`research-report/metrics_research.md`（Phase B 对域适配提取器的调研结论——ImageNet 预训练提取器排名稳定、医学预训练有争议）。
