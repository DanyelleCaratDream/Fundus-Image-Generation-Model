# transfer_learning/ —— 迁移学习方法

> Phase C（Bug 2 拓展）：利用预训练权重/其他任务权重迁移到眼底图生成或特征提取。
> 与 `pretrained/`（预训练生成，如 SD LoRA）区分：这里侧重"迁移"范式（权重初始化、域适配、特征提取器复用）。

**状态**：📌 占位，尚未开始。与 `pretrained/` 一样，启动条件为 Phase C2（传统 ML）完成后按需评估。

**资料**：`report-tl-lit.md` —— 迁移学习文献与定位归档（Phase B 结论 + 小样本注意事项），供 Phase C 启动前查阅。

**参考**：`research-report/evaluation_report.md`（Phase B 对域适配提取器的调研结论——ImageNet 预训练提取器排名稳定、医学预训练有争议）。
