# machine_learning/ —— 传统机器学习 / OpenCV 方法

> Phase C2（老师 Bug 2）：非深度学习的生成方法，如 OpenCV + 传统 ML（图像变换/特征合成/形态学等）。

**状态**：📌 占位，尚未开始。这是验证"生成质量是否依赖深度学习"的关键对照，评估结果将直接用根目录评分体系打分对比（丢 JSON 进 `eval_data/` → `python eval/score_scheme.py --scorecard`）。

**参考**：`docs/09-Score-Scheme-Design.md`（新方法接入流程）。
