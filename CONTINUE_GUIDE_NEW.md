# Continue Guide - 2026-07-28

> **当前状态：FiLM DDPM + L1 + LPIPS 780 轮训练完成，达 85~90/100 分**
> **下一步：梯度检查点 + 推理后处理管线 + 损失函数再优化**

## 已完成实验

| 模型 | 评分 | 说明 |
|------|:----:|------|
| VAE | 10/100 | MSE 模糊天花板 |
| DCGAN / WGAN-GP | 20/100 | 判别器过拟合 |
| StyleGAN2-ADA | — | CUDA 环境不兼容 |
| 无条件 DDPM | 75/100 | 结构对但缺细节 |
| 条件 DDPM (MSE) | 70/100 | 220 轮巅峰，之后溶解 |
| Palette (MSE) | 0/100 | 125 轮巅峰→275 模糊→推理完全无效 |
| **FiLM DDPM (MSE, 500轮)** | **85/100** | **延缓溶解到 450 轮** |
| **FiLM DDPM + L1 + LPIPS (780轮)** | **85~90/100** | **最佳方案，700 轮效果最突出** |

## 最新实验结果

### FiLM DDPM + L1 + LPIPS（780 轮全链训练）

- **损失函数**：L1（噪声预测）+ LPIPS（t<200，权重 0.1）
- **EMA decay**：0.9999
- **训练策略**：交替使用 ORIGINAL（330 张高质量原图）和 much（1320 张增强版）
  - ORIGINAL 180 轮 → much 60 轮 → ORIGINAL 180 轮 → ... 共 780 轮
- **最终 loss**：L1 ~0.048~0.055（等价 MSE ~0.002~0.003）
- **最佳 epoch**：~700 轮

### 效果评价
- ✅ 病灶（白斑/出血点）保持能力大幅提升，未见明显溶解
- ✅ 血管纹理锐利度显著改善（LPIPS 感知损失的作用）
- ⚠️ 部分图像有白色斑点伪影（LPIPS 在某些高频特征上过度激活）
- ⚠️ 部分图像边缘区域仍有未收敛噪点
- 总体：约 60~70% 样本达用标准（85~90/100），30~40% 因伪影/噪点待优化

## 后续优化方向（按实现难度排序）

### 1️⃣ 推理时后处理（最容易，无需重新训练）
- 中值滤波或 DnCNN 去白色斑点伪影
- 超分辨率 128→256（ESRGAN/SwinIR），同时增强纹理
- `--color_correct` 颜色精校正已可用

### 2️⃣ 梯度检查点（Gradient Checkpointing）
- 可降低显存 30~50%，支持 base_dim 从 128 提升到 160/192
- 或维持架构将 batch_size 翻倍
- PyTorch 原生支持，改动量小

### 3️⃣ 损失函数再优化
- **边缘感知损失**：Sobel/Canny 边缘 L1 损失，减少白色伪影
- **多尺度 LPIPS**：在 128/64/32 多尺度计算 LPIPS
- **对抗性损失**：在 x_0_pred 上接小型判别器（需注意 GAN 小样本不稳定性）

### 4️⃣ 多尺度条件注入（改进最大，改动量最大）
- 类似 ControlNet，在 UNet 多个分辨率层分别注入条件特征
- 条件图从二值骨架改为连续值概率图/距离变换图

### 5️⃣ 自监督预训练（需要更多数据）
- 在大量无标注眼底图（10000+ 张）上预训练 UNet
- 再用 330 张标注骨架图微调
- 理论提升最大，但受限于数据获取

### 报告

| 文件 | 大小 | 说明 |
|------|:----:|------|
| `research-report/REPORT.docx` | 9.5 MB | 含全部实验章节 + 嵌入图（含 L1+LPIPS 700~775 图） |
| `research-report/generate_docx.py` | — | 报告生成脚本 |

### 输出目录

| 目录 | 内容 |
|------|------|
| `Fundus-Diffusion/ddpm/results_film_l1lpips/` | 780 轮全链训练结果 |
| `.../images/` | 预览图（epoch 25~775，间隔 25） |
| `.../models/` | 每 60 轮 checkpoint + 最终模型 |
| `.../logs/` | 训练日志 + Loss 曲线图 |
